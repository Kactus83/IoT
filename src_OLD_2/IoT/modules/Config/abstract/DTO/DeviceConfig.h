#ifndef DEVICE_CONFIG_H
#define DEVICE_CONFIG_H

struct DeviceConfig {
    const char* DEVICE_NAME;
    const char* DEVICE_UNIQUE_ID;
    const char* INTEGRATION_NAME;
    const char* STANDALONE_PASSWORD;
};

#endif // DEVICE_CONFIG_H
